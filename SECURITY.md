# 자격증명 취급 규칙

이 저장소는 **Public** 입니다. API 키가 한 번 커밋되면 봇이 몇 분 안에 긁어갑니다.
git 이력에서 지우는 것도 번거로우니 애초에 올라가지 않게 합니다.

## 1. 커밋 차단 훅 설치 (한 번만)

저장소 루트에서:

```bash
git config core.hooksPath .githooks
```

이걸 설정하면 커밋할 때 아래 값이 섞여 있는지 검사하고, 발견되면 **커밋을 막습니다.**

- Google API 키 (`AIza...`) — Gemini
- GitHub 토큰 (`ghp_` / `gho_` / `ghs_` / `ghu_`)
- OpenAI 키 (`sk-...`)
- AWS 액세스 키 (`AKIA` / `ASIA`)
- 개인키 블록 (`BEGIN ... PRIVATE KEY`)
- 설정 파일에 값을 직접 적은 경우 (환경변수 `${...}` 참조는 통과)
- `.env` 파일 (`.env.example` 은 허용)

오탐이면 `git commit --no-verify` 로 넘길 수 있지만, 넘기기 전에 정말 키가 아닌지
한 번 더 보세요.

> 훅은 각자 로컬에 설정해야 합니다. `git config` 는 저장소에 저장되지 않으므로
> **clone 한 사람마다 위 명령을 한 번씩** 실행해야 합니다.

## 2. 키는 환경변수로만

설정 파일에 값을 적지 않습니다. `application.yaml` 은 이렇게 되어 있습니다.

```yaml
app:
  llm:
    api-key: ${GEMINI_API_KEY:}   # 기본값 없음 — 없으면 기능이 꺼질 뿐
```

Windows 에서 설정 (새 터미널부터 적용):

```powershell
setx GEMINI_API_KEY "발급받은_키"
```

## 3. 프론트엔드에 키를 두지 마세요

LLM 키는 **백엔드에만** 있어야 합니다. 프론트엔드 코드에 넣으면 브라우저
개발자도구에서 그대로 보입니다.

```
프론트 → 백엔드 → Gemini      ← 이 구조를 지킵니다
프론트 → Gemini               ← 금지 (키가 노출됨)
```

## 4. 키가 노출됐다면

채팅·메신저·스크린샷·커밋 등 **어디든 한 번 나갔으면 폐기하고 새로 발급**하는 게
원칙입니다. 사용량 제한이 걸릴 뿐 아니라, 그 키가 결제 연결된 프로젝트 소속이면
요금이 청구될 수 있습니다.

- Google AI Studio: https://aistudio.google.com/apikey 에서 삭제 후 재발급
- 무료 티어라도 **입력 데이터가 모델 개선에 사용될 수 있습니다.** 실제 사업장 정보를
  넣는 단계에서는 팀이 이 점을 알고 결정해야 합니다.

## 5. 해커톤 종료 후

제출이 끝나면 사용하던 키를 삭제하세요. 저장소는 계속 공개된 채로 남습니다.
